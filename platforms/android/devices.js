// Convenience re-export. EDIT THE LIST in automation.config.js (android.devices)
// — this just surfaces it on a stable per-platform path for the engine.
module.exports = require("../../automation.config").android.devices;
